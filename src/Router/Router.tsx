import { BrowserRouter, Outlet, Routes} from 'react-router-dom';

function Router() {
  return (
    <>
    <BrowserRouter>
    <Routes>
    </Routes>
    </BrowserRouter>
    < Outlet />
    </>
  )
}

export default Router